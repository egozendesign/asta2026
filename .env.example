# 21st.dev Magic MCP — chiave API letta da .mcp.json come ${TWENTYFIRST_API_KEY}
# Genera/rigenera la key su https://21st.dev (console) e NON committarla.
# Copia questo file in .env e inserisci la tua key, oppure esporta la variabile
# nel tuo shell prima di avviare Claude Code / l'editor.
TWENTYFIRST_API_KEY=
