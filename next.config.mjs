/** @type {import('next').NextConfig} */
const nextConfig = {
  // Il logo è un PNG grande servito da /public: nessuna configurazione extra.
  // Le API sono gestite da app/api/state/route.js (runtime Node.js).
};

export default nextConfig;
